altgraph==0.17.4
certifi==2025.1.31
charset-normalizer==3.4.1
et_xmlfile==2.0.0
idna==3.10
joblib==1.5.1
lief==1.0.0
numpy==2.3.2
openpyxl==3.1.5
packaging==25.0
pandas==2.3.2
pefile==2023.2.7
pillow==12.1.1
psutil==6.1.1
pyarrow==21.0.0
pyasn1==0.6.1
pyinstaller==6.15.0
pyinstaller-hooks-contrib==2025.8
PySimpleGUI==5.0.8.3
python-dateutil==2.9.0.post0
pytz==2025.2
pywin32-ctypes==0.2.3
PyYAML==6.0.3
requests==2.32.3
rsa==4.9.1
scikit-learn==1.7.1
scipy==1.16.1
setuptools==80.9.0
six==1.17.0
threadpoolctl==3.6.0
tzdata==2025.2
urllib3==2.3.0
yara-python==4.5.1

# Genuinely optional, and deliberately left uninstalled.
#
# Every one of these has a working degradation path, unlike lief and PyYAML
# above -- those two were undeclared for a while and each silently cost the
# pipeline a capability (a scoring input, and half the accepted spec formats).
# Listed here so "not installed" stays a decision on record rather than a gap
# nobody noticed.
#
#   tqdm        progress bars only; steps.py falls back to None
#   tlsh        fuzzy hash; steps._best_effort_tlsh returns None without it
#   ssdeep      fuzzy hash; steps._best_effort_ssdeep returns None without it
#               (needs a compiler on Windows, which is why it is not pinned)
#   weasyprint  direct PDF output; without it, print the HTML report from a
#               browser instead
