# The exact bench environment, pinned. `pyproject.toml` declares what the
# package needs to import; this reproduces one machine, transitive pins and
# all. Install with `pip install -r requirements.txt` to rebuild the bench,
# or `pip install -e ".[dev]"` to work on the engine.
#
# Genuinely optional, and deliberately left uninstalled. Listed here so "not
# installed" stays a decision on record rather than a gap nobody noticed.
#
#   ssdeep      fuzzy hash; steps._best_effort_ssdeep returns None without it
#               (needs a compiler on Windows, which is why it is not pinned)
#   tlsh        fuzzy hash; steps._best_effort_tlsh returns None without it
#   tqdm        progress bars only; steps.py falls back to None
#   weasyprint  direct PDF output; without it, print the HTML report from a
#               browser instead
#
# Every one of these has a working degradation path, unlike lief and PyYAML
# below -- those two were undeclared for a while and each silently cost the
# pipeline a capability (a scoring input, and half the accepted spec formats).
#
# **PySimpleGUI was removed 04 Sep.** Version 5 is licensed Proprietary and
# nothing in this repository imports it -- it was left behind by a `pip
# freeze`. A reader auditing this file for a commercial release would have
# had to establish that themselves. See THIRD-PARTY-NOTICES.md.

altgraph==0.17.4
certifi==2025.1.31
charset-normalizer==3.4.1
et_xmlfile==2.0.0
idna==3.10
joblib==1.5.1
lief==1.0.0
numpy==2.3.2
openpyxl==3.1.5
packaging==25.0
pandas==2.3.2
pefile==2023.2.7
pillow==12.1.1
psutil==6.1.1
pyarrow==21.0.0
pyasn1==0.6.1
pyinstaller-hooks-contrib==2025.8
pyinstaller==6.15.0
python-dateutil==2.9.0.post0
pytz==2025.2
pywin32-ctypes==0.2.3
PyYAML==6.0.3
pyzipper==0.3.6
requests==2.32.3
rsa==4.9.1
scikit-learn==1.7.1
scipy==1.16.1
setuptools==80.9.0
six==1.17.0
threadpoolctl==3.6.0
tzdata==2025.2
urllib3==2.3.0
yara-python==4.5.1
